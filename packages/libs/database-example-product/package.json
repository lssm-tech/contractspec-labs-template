{
  "name": "@lssm/lib.database-example-product",
  "version": "0.4.0",
  "scripts": {
    "clean": "rm -rf dist src/generated",
    "lint": "bun run lint:fix",
    "lint:fix": "eslint src --fix",
    "lint:check": "eslint src",
    "publish:pkg": "bun publish --tolerate-republish --ignore-scripts --verbose",
    "publish:pkg:canary": "bun publish:pkg --tag canary",
    "build": "bun run build:bundle",
    "build:types": "tsc --noEmit",
    "build:bundle": "tsdown",
    "dev": "bun run build:bundle --watch",
    "prisma:import": "prisma validate",
    "prisma:check": "prisma validate",
    "db:migrate": "prisma migrate dev",
    "db:deploy:off": "prisma migrate deploy",
    "db:deploy": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:seed": "echo \"No seed configured (use prisma/seed.ts if added)\"",
    "db:reset": "prisma db push --skip-generate --force-reset && bun run db:seed",
    "db:reset:full": "bun run db:generate && bun run db:reset",
    "generate": "bun run db:generate"
  },
  "peerDependencies": {
    "@prisma/client": "^7.1.0"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.1.0",
    "@prisma/client": "^7.1.0"
  },
  "devDependencies": {
    "@lssm/app.cli-database": "canary",
    "prisma": "^7.1.0",
    "tsdown": "^0.17.4"
  },
  "main": "./dist/index.mjs",
  "exports": {
    ".": "./dist/index.mjs",
    "./client": "./dist/client.mjs",
    "./enums": "./dist/enums.mjs",
    "./models": "./dist/models.mjs",
    "./*": "./*"
  },
  "typesVersions": {
    "*": {
      "*": [
        "src/*"
      ]
    }
  },
  "files": [
    "src",
    "dist",
    "generated"
  ],
  "type": "module",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "publishConfig": {
    "access": "public",
    "exports": {
      ".": "./dist/index.mjs",
      "./client": "./dist/client.mjs",
      "./enums": "./dist/enums.mjs",
      "./models": "./dist/models.mjs",
      "./*": "./*"
    }
  }
}
